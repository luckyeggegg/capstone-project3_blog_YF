<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
 
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Noto+Serif:ital,wght@0,100..900;1,100..900&family=Suranna&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <link rel="stylesheet" href="/styles/main.css">
  <!--CSS file should be placed under the Bootstrap link otherwise the CSS effects will be overidden.-->
  <!--An absolute path "/styles/main.css" rather than a relative path "styles/main.css" must be used here. This is because：The issue you're encountering is related to how relative paths are resolved in web browsers. When you include a relative path like href="styles/main.css" in your header.ejs, the browser will resolve this path relative to the current URL in the address bar.
In the case of the "newPost.ejs" page, if the URL is something like http://localhost:3000/newPost, the browser will correctly look for the CSS file at http://localhost:3000/styles/main.css.
However, for the "editPost.ejs" page, if the URL includes additional path segments like http://localhost:3000/editPost/123, the browser will mistakenly look for the CSS file at http://localhost:3000/editPost/styles/main.css, which doesn't exist, hence the error about MIME types.
To fix this issue, you should use an absolute path for your CSS file by prefixing it with a forward slash. This tells the browser to resolve the path from the root of the domain, ensuring the correct URL is constructed regardless of the current page's URL.-->
  <title>YF's Blog</title>
</head>
  
<body>
  <div class="container">
    <header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom">
      <a href="/" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto link-body-emphasis text-decoration-none">
        <span class="fs-1">YF's Blog</span>
      </a>

      <ul class="nav nav-pills">
        <li class="nav-item"><a href="/" class="nav-link active" aria-current="page">Home</a></li>
        <!-- <li class="nav-item"><a href="#" class="nav-link">Features</a></li>
        <li class="nav-item"><a href="#" class="nav-link">Pricing</a></li>
        <li class="nav-item"><a href="#" class="nav-link">FAQs</a></li>
        <li class="nav-item"><a href="#" class="nav-link">About</a></li> -->
      </ul>
    </header>
  </div>
 